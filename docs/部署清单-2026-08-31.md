# 2026-08-31 改动汇总与部署清单

本文汇总 2026-08-31 对 MoveClan 的改动，以及部署到线上所需执行的操作。

## 一、今日改动内容（未提交）

| 版本 | 内容 |
|---|---|
| v1.9.3 | 趋势新增「**周**」周期：周一~周日、每日平均；‹ › 按周切换（不可切未来周）；点日期锚点跳所在周；x 轴与气泡显示周一到周日 |
| v1.9.4 | 运动类型扩至 **24 种**，新增 爬楼(8.0)/HIIT(9.0)/力量训练(5.5)/普拉提(3.0)/搏击操(7.8)（MET 参考《体力活动纲要》） |
| v1.9.5 | 排行榜同指标破平：**次数 > 卡路里 > 运动时长** |
| v1.10.0 | **BMI 环形指标卡**：Canvas 2D 270° 环形仪表（深色科技医疗风），四段分色弧带 + 刻度 10–40 + 中心枢纽（BMI/分类居中、身高体重在圆环下方）+ 发光弧线三角指针（0.7s 缓动）+ 图例四档完整范围；替代「我的」页身高/体重/BMI 数据栏；新增独立测试页 `pages/bmi-gauge-test` 与 `tools/bmi-gauge-preview.html` |

> 说明：本清单只列今日（08-31）改动；08-27 及其前改动见 `docs/部署清单-2026-08-27.md`。

## 二、部署操作步骤

### 第 1 步：部署云函数（云端安装依赖）

| 云函数 | 说明 | 操作 |
|---|---|---|
| `submitCheckin` | 运动类型白名单 24 种 | 重部署 |
| `updateCheckin` | 运动类型白名单 24 种 | 重部署 |
| `getGroupRanking` | 排行榜同指标破平（次数>卡路里>时长） | 重部署 |

### 第 2 步：数据库

- 无改动（`weight_records` 等集合如需未建，见 08-27 清单）。

### 第 3 步：重新编译前端

涉及 `weight-chart` 组件（周周期）、`profile`（周导航/锚点跳周）、`constants.js`（运动类型 24 种）、`bmi-gauge` 组件与 `profile`（BMI 环形卡，含测试页注册）。

## 三、验证清单

1. 趋势卡周期含「**周**」：周一~周日每日平均；‹ › 切周、未来周不可切；点日期锚点跳所在周；触摸点弹 `周几：数值`
2. 打卡页运动类型含 爬楼/HIIT/力量训练/普拉提/搏击操
3. 排行榜按所选指标排序，同指标按 次数>卡路里>时长 破平
4. **BMI 环形卡**：「我的」页显示环形仪表；165/67→24.6 超重(橙)、165/80→29.4 肥胖(红)；指针平滑转动、数值滚动、中心 BMI/分类对称、身高体重在圆环下方、图例显示四档范围；测试页可单独打开

## 四、备注

- 今日改动尚未 commit，部署前需先提交。

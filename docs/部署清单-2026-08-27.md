# 2026-08-27 改动汇总与部署清单

本文汇总 2026-08-27 对 MoveClan 的所有改动，以及部署到线上所需执行的操作。

## 一、今日改动内容

### 已提交
| 版本 | commit | 内容 |
|---|---|---|
| v1.7.9 | `008761e` | 排行底部遮挡（padding 160→240rpx）；我的页去掉累计打卡次数、`getMyStats` 补 `totalCalories`（总卡路里不显示修复） |
| v1.8.0 | `f482861` | 最近打卡/动态时间显示到秒（`date.js` 新增 `formatDateTime`，北京时区） |
| v1.8.1 | `d371684` | 隐藏「我的」页订阅消息设置（预留接口，清理 UI/逻辑/样式） |

### 未提交（体重曲线 + 身高/BMI/趋势）
| 内容 | 说明 |
|---|---|
| 体重曲线 | 新增 `weight_records` 集合、`getWeightRecords` 云函数、`weight-chart` Canvas 组件；`login` 修改体重时写入历史 |
| 身高 | `users.heightCm`，默认/回填 **170**，编辑资料可改，头部显示身高 |
| BMI | 中国标准分类显示（<18.5 偏瘦 / 18.5-23.9 正常 / 24-27.9 超重 / ≥28 肥胖） |
| 趋势卡 | 指标切换（体重\|BMI）× 周期（日\|月\|年）× 时间导航（不可切未来）；日=24 小时槽、月=当月天数槽、年=12 个月槽；无更新槽不打点、单点不成线；触摸点弹 `时间：数值` 气泡 |
| 数据 | `getWeightRecords` 修复排序（>60 条丢最新，改倒序取 500 再反转）；`login` 为老用户插起始基线（趋势不断档） |

## 二、部署操作步骤

### 第 1 步：部署云函数（云端安装依赖）

| 云函数 | 说明 | 操作 |
|---|---|---|
| `getMyStats` | 补 `totalCalories` 返回 | 重部署 |
| `login` | 身高读写 + 默认 170 回填 + 老用户体重起始基线 | 重部署 |
| `getWeightRecords` | 读取体重历史（按时间升序，最近 500 条） | **新增** |

### 第 2 步：数据库（云开发控制台）

- **新建集合 `weight_records`**：权限「仅管理端可读写」，索引 `(openid, createTime)`（详见 `database/weight_records.init.json`）
- `users`：无需迁移（`heightCm` 为可选字段，老用户下次登录自动回填 170；有体重但无记录时自动插入起始基线）

### 第 3 步：重新编译前端

涉及 `profile`（身高/BMI/趋势卡）、`weight-chart` 组件、`index`/`feed`（时间到秒）、`ranking`（底部 padding）、`date.js`、`mock.js` 等。

## 三、验证清单

1. 我的页：显示「体重 xx kg · 身高 xx cm」；BMI + 分类标签
2. 趋势卡：体重/BMI 切换、日/月/年切换；‹ › 翻页、未来不可切；触摸数据点弹出 `时间：数值` 气泡；单点不成线
3. 老用户登录后趋势自动出现起始点（基线），改体重后开始画线
4. 最近打卡/动态时间显示到秒
5. 排行页末位成员不被「我的排名」栏遮挡
6. 我的页无「设置/订阅消息」区块

## 四、备注

- 未提交的体重曲线/身高/BMI/趋势改动需先 commit 再部署（当前在工作区）。
- `logo1.png`（未使用备用图）未跟踪，无需处理。

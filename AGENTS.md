# AGENTS.md — 项目工作约定

本文件用于告知协作代理（AI）在本仓库内工作时必须遵守的约定。

## Git 推送规则

- **执行 `git push` 前，必须先询问用户并获得明确同意**，不得擅自推送。
- 允许自主执行：`git status`、`git diff`、`git log`、`git add`、`git commit`（提交前确认改动范围正确即可）。
- 推送时机以用户明确表示「推送 / 可以推送 / 发布」为准。

## 其他约定

- 详细部署说明见 `CHANGELOG.md` 与 `docs/部署清单-*.md`。
- 维护文档（CHANGELOG、问题修复记录、部署清单）随代码改动同步更新。
